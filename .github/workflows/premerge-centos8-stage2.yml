name: Pre-merge CentOS Stage2

on:
  workflow_dispatch:
  push:
    paths:
      - 'scripts/cnode-helper-scripts/cabal-build-all.sh'
      - 'containers/pre-merge/centos8-stage1_2.containerfile'
      - '.github/workflows/premerge-centos8-stage1_2.yml'
  pull_request:
    branches: [ development ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: container login
      run: |
        docker login -u ${{ secrets.DOCKER_HUB_USERNAME }} -p ${{ secrets.DOCKER_HUB_PASSWORD }}
    - name: Compiling new node software
      run: |
        docker build . --file containers/guild-ops-base/stage2.containerfile --compress --tag chaincrucial/guild-ops-base:stage2
